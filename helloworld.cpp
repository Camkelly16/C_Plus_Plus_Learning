#include <iostream> // Header file that contain function for basic inupt and output operations

int main(){ //main function is where the program begins 

// std = standard. 
//c = character. out = output, << = output. 
//Put semicolon at the end of each statement to finish it
// << std::endl = to end the line and start a new one
// '\n' = to end the line and start a new one

    std::cout << "I like pizza!" << std::endl; 
    std::cout << "It's really good" << '\n';
    std::cout << "I am 125 lb";

    return 0; // If your code reach return zero that mean that there was no programs in your program
}
